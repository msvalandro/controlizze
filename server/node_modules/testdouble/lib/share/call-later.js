'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = callLater;

var _lodash = require('../wrap/lodash');

var _lodash2 = _interopRequireDefault(_lodash);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _toConsumableArray(arr) { if (Array.isArray(arr)) { for (var i = 0, arr2 = Array(arr.length); i < arr.length; i++) { arr2[i] = arr[i]; } return arr2; } else { return Array.from(arr); } }

function callLater(func, args, defer, delay) {
  if (delay) {
    _lodash2.default.delay.apply(_lodash2.default, [func, delay].concat(_toConsumableArray(args)));
  } else if (defer) {
    _lodash2.default.defer.apply(_lodash2.default, [func].concat(_toConsumableArray(args)));
  } else {
    func.apply(undefined, _toConsumableArray(args));
  }
}